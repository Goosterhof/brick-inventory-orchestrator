# Inspection Report: Post-merge review — PRs #225, #226, #227

**Report #:** 2026-05-16-post-merge-prs-225-226-227
**Filed:** 2026-05-16
**Inspector:** Building Inspector
**Scope:** Targeted: sets overview filter fix sequence + EntryNotFoundError test coverage
**Pulse Version:** Assessed 2026-03-29
**Triggered By:** CEO request — review last-24h commits

---

## Quality Gauntlet Results

_Gauntlet not run — Node 24 unavailable in this remote environment (node_modules absent).
Findings below are drawn from static code review of the three commits._

| Check         | Result  | Notes                           |
| ------------- | ------- | ------------------------------- |
| format:check  | Not run | Node 24 required, not available |
| lint          | Not run | Node 24 required, not available |
| lint:vue      | Not run | Node 24 required, not available |
| type-check    | Not run | Node 24 required, not available |
| test:coverage | Not run | Node 24 required, not available |
| knip          | Not run | Node 24 required, not available |
| size          | Not run | Node 24 required, not available |

---

## Commits Reviewed

| Commit    | PR   | Title                                                                                  |
| --------- | ---- | -------------------------------------------------------------------------------------- |
| `5c1a8a3` | #226 | fix(sets): read set_num from nested set in overview filter                             |
| `c651cf6` | #225 | test(storage,sets): cover EntryNotFoundError catch branches; drop storage.loadingParts |
| `e94c582` | #227 | fix(sets): restore setNum fallback for wishlist sets in overview filter                |

---

## Findings

### Category: Tests / Correctness

1. **Incomplete fix shipped in #226, required same-day follow-up in #227** `severity: medium`
    - **Location:** `src/apps/families/domains/sets/pages/SetsOverviewPage.vue:68`
    - **Standard:** Ship-complete fixes; tests cover the full input space before merge
    - **Observation:** PR #226 changed the filter from `s.setNum.toLowerCase()` to `(s.set?.setNum ?? '').toLowerCase()`. This fixed the TypeError for normal sets (where `s.set?.setNum` is populated) but silently broke wishlist sets — which carry no nested `set` — by falling back to `''`, making them invisible to any search. PR #227 corrected this to `?? s.setNum` the next morning. The root cause: #226 was written from the perspective of the normal-set code path only; the wishlist code path was not considered until a test was written later.
    - **Recommendation:** Before merging a fix to a filtered list, enumerate all item subtypes (owned, wishlisted, sealed, etc.) and add at least one test for each subtype's field access pattern. The existing `status: 'wishlist' as const` mock in the spec suite provides a ready-made fixture.

2. **Missing terminal null guard on `setNum` in search filter** `severity: low`
    - **Location:** `src/apps/families/domains/sets/pages/SetsOverviewPage.vue:68`
    - **Standard:** Defensive null access; paired lines should use the same guard pattern
    - **Observation:** Current code: `const setNum = (s.set?.setNum ?? s.setNum).toLowerCase()`. The adjacent `name` line uses a terminal `?? ''` guard: `const name = (s.set?.name ?? '').toLowerCase()`. If `s.setNum` is ever `undefined` (e.g., a newly-constructed domain object before persistence), `.toLowerCase()` will throw. In practice `setNum` is the primary identifier and should always be populated, but the asymmetry with the `name` line is a latent inconsistency.
    - **Recommendation:** Change to `(s.set?.setNum ?? s.setNum ?? '').toLowerCase()` for parity with the `name` guard. One-character fix, eliminates the asymmetry.

### Category: Debt

3. **Two anti-pattern sites explicitly deferred with no permit** `severity: low`
    - **Location:** `src/apps/families/domains/sets/pages/SetsOverviewPage.vue:107, 177`
    - **Standard:** Deferred follow-up work requires a building permit (Operations Manual)
    - **Observation:** Commit 5c1a8a3's message explicitly names two adjacent sites with the same `set?.X` anti-pattern that were "intentionally left for a follow-up": (a) the `flatSets` sort comparator at line 107 (`a.set?.name`) and (b) the `toCsvRow` helper at line 177 (`set?.setNum ?? s.setNum` — yields `'undefined'` in CSV for wishlist sets). No permit was filed for this work.
    - **Recommendation:** File a low-priority building permit for these two sites. The CSV issue is cosmetically wrong (exports `'undefined'` instead of the actual set number) and worth fixing to keep exports clean.

### Category: Architecture

4. **Coverage gap in `src/apps/**/domains/**` is now documented but not tracked in pulse** `severity: low`
    - **Location:** `src/tests/` coverage config; `.claude/docs/pulse.md`
    - **Standard:** Known architectural limitations belong in the pulse Tech Debt register
    - **Observation:** Commit c651cf6's message explicitly notes that the narrowed `catch` branches introduced in commit `81f6455` had zero test coverage for three weeks, and that the vitest coverage config excludes `src/apps/**/domains/**` from thresholds — making such gaps invisible to the automated 100% gate. The commit acknowledges this is a known tradeoff (domains are integration-tested separately), but the pulse does not record it.
    - **Recommendation:** Add an entry to the pulse Tech Debt register: "Domain pages excluded from unit-coverage 100% gate — catch-branch gaps require manual inspection or a separate domain-coverage threshold." This is a process gap, not a code gap, but it belongs in the record.

---

## Positives

These are worth calling out explicitly as evidence of good practice:

- **Root-cause diagnosis in commit messages.** Both `5c1a8a3` and `c651cf6` contain full causal chains (not just "what changed" but "why the old code behaved the way it did"). The observation that Vue 3 silently returns the previous computed value on a throw — making the bug look like "search doesn't filter" rather than "search crashes" — is the kind of insight that prevents future engineers from re-diagnosing the same class of bug.
- **Consistent two-test pattern for the catch branches.** The `EntryNotFoundError` path test and the re-throw test are structurally identical across all four pages (`EditSetPage`, `SetDetailPage`, `EditStoragePage`, `StorageDetailPage`). The timing pattern (set `errorHandler` after `shallowMount`, before `flushPromises`) is correct and shows understanding of Vue's async lifecycle.
- **Translation key cleanup was surgical.** `storage.loadingParts` was removed from both EN and NL, and the commit message correctly distinguishes it from `sets.loadingParts` (which is still in use). No over-deletion.
- **oxfmt style collapses correctly attributed.** The `sortLabelKey` record collapse in `PartsUnsortedPage.vue` and the `defineEmits` collapse in `SetListItem.vue` are tiny but correctly labelled as oxfmt-enforced and bundled with a rationale.

---

## Doc Drift

| Document           | Accurate | Drift Found                                                         |
| ------------------ | -------- | ------------------------------------------------------------------- |
| Domain Map         | Yes      | None observed                                                       |
| Component Registry | Yes      | c651cf6 updated `component-registry.json` (SetListItem emit change) |
| Pulse              | Partial  | Coverage-gap limitation not in Tech Debt register (see F-4)         |
| CLAUDE.md          | Yes      | None observed                                                       |

---

## Proposed Pulse Updates

1. **Tech Debt — add:** "Domain pages excluded from unit-coverage 100% gate — `src/apps/**/domains/**` is excluded from thresholds; catch-branch gaps in domain pages require manual inspection or post-inspection test sweeps to surface."

---

## Summary

**Overall Health:** 8/10
**Findings:** 4 total (0 high, 1 medium, 3 low)
**Showcase Readiness:** Portfolio-ready
**Recommendation:** Targeted fixes — address F-1's recommendation (enumerate item subtypes in filter tests), apply the one-line F-2 guard, file the F-3 permit, and update the pulse for F-4.

The fix sequence itself is sound and the diagnostic quality in the commit messages is excellent. The medium finding is process-level (incomplete fix shipped without full subtype coverage) rather than a runtime defect — the code is correct as of `e94c582`.

---

## Self-Debrief

### What I Caught

- The two-commit fix sequence (incomplete fix → follow-up) is the clearest signal in this batch and worth naming explicitly as a process pattern to avoid.
- The terminal null guard asymmetry is subtle but real — easy to miss without reading both lines side-by-side.
- The deferred anti-pattern sites were documented in the commit message but no permit was filed; the Operations Manual is unambiguous on this.

### What I Missed

- Could not run the quality gauntlet (Node 24 unavailable). Static review only — no coverage numbers, no lint output, no type errors confirmed absent.
- Did not inspect the integration test at `e2e/tests/family-sets.spec.ts:117` referenced in the commit message (orchestrator repo, not in scope here).

### Methodology Gaps

- No runtime environment means findings rely entirely on reading code. Linting and type-checking might surface additional issues not visible to static review.

### Training Proposals

| Proposal                                                                                     | Context                                                                      | Report Evidence                                    |
| -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------- |
| When reviewing a filter-computed fix, enumerate all item subtypes as a review checklist item | The wishlist subtype was missed in #226's fix; #227 was a same-day follow-up | This report #2026-05-16-post-merge-prs-225-226-227 |
| Paired null-guard lines in the same computed should use the same terminal fallback pattern   | `name` had `?? ''`, `setNum` did not                                         | This report #2026-05-16-post-merge-prs-225-226-227 |

---

## CFO Evaluation

_To be appended by the CFO._
