# Shift 008 — 2026-07-16

**Steward on duty** · Hunt: patrol rotation 5 (diff-hotspots, gallery-auth, foundry-authz, stud-connections) · Loop: shift 8 of run started 2026-07-16

## Roll-Call
- Board at clock-in: 14 in To Do, 0 in progress, 2 in review (BIO-0023/0025 → PRs #290/#291) · Review queue: 3 (#284, #290, #291) — gate open
- Carried from last shift: clean handoff; no merges since shift 007.

## Hunt
- `shift-patrol` (rotation 5 — first pass over auth/authz territory): 6 agents, ~467k tokens. **Confirmed: 2 (both medium, Gallery), refuted: 0**, low observations: 5. **foundry-authz came back clean** — no tenant-scope or policy findings survived.
- diff-hotspots re-verified all five of today's merges sound at the vendor level (retry-loop throw semantics, ms→µs conversion, dedupe map append).

## Issues Filed
- **BIO-0026** — No 401 response-error middleware: mid-session expiry strands the user; docs/showcase describe middleware that doesn't exist (medium, Gallery, `bug`; folds in GA-3 doc claim + GA-5 logout-failure state leak).
- **BIO-0027** — Families app boot hard-fails to a blank page on any non-401 `/me` error (medium, Gallery, `bug`).
- **BIO-0028** — Storage grid dimensions backend-complete but unreachable from the Gallery; EditStoragePage patch would wipe API-seeded grids (low→filed on third recurrence, Gallery, `inconsistency`; sibling of BIO-0018).

## Builds
- **BIO-0026 + BIO-0027 → PR #292** — green: pushed + labeled. Built together on `fix/BIO-0026-BIO-0027-auth-session-resilience` (both touch `auth/index.ts`; separate PRs would have guaranteed a conflict), commit 97e296e, 10 files +521/−17. New `registerAuthErrorMiddleware` (401 → `clearUser()` + login redirect only from authOnly routes; boot probe and failed logins no-op via the isLoggedIn guard); `logout()` clears state in `finally`; `checkIfLoggedIn()` degrades any axios failure to logged-out so the app always mounts. 8-case middleware unit spec + 3-flow integration spec on real services; harness gained `onGetError`. Full Gallery gauntlet green (118 files/1452 unit, 20 files/149 integration, 100% coverage). Deviation flagged on the issue: wired in `main.ts` (import-cycle avoidance), not `apps/families/services/`.

## Observations (low severity, not filed)
1. **D-theme-1** (new): `ThemeResourceData` emits camelCase `parentId` — the lone deviation from the all-snake_case ResourceData wire convention; #279's commit comment describes the wire shape wrongly (no snake key ever exists for theme). Idempotent under deepCamelKeys so no runtime failure; mixed-case JSON in one response is a showcase flag. Recurrence 1.
2. **GA-4** (new): auth-guard redirect drops the intended destination; `fromQuery` middleware reads `?from=` that no producer ever sets. Post-login deep-link return is silently unimplemented. Recurrence 1.
3. Carried: P-scan-add-hydrate-1 (rec 1, on BIO-0016 trail), F-tx-2 SyncThemes pass-2 no transaction (rec 1), SC-storagemap-202-1 (rec 1), F-txn-2 cascade-substring (rec 2), P-forms-2 (rec 1), P-recv-3 (rec 1). SC-2 deduped (= BIO-0022, already filed).

## For the CEO
**Review queue: 4 — #292 (the auth-resilience pair: session expiry no longer strands users, boot no longer blank-pages), #291 (storage race), #290 (docs, 2 min), #284 (external pilot, oldest).** The patrol's first pass over auth territory found the two most user-visible bugs of the run so far; foundry authz is clean. Backlog decisions still parked: BIO-0007, BIO-0017, BIO-0024; BIO-0008 (parts composables) remains the recommended next build.
