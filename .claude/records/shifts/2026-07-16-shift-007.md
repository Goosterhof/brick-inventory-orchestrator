# Shift 007 — 2026-07-16

**Steward on duty** · Hunt: patrol rotation 4 (diff-hotspots, gallery-forms, foundry-transactions, stud-connections) · Loop: shift 7 of run started 2026-07-16

## Roll-Call
- Board at clock-in: 16 in To Do, 0 in progress, 5 in review · Review queue at clock-in: **1** (#284 only) — **the CEO merged all five shift PRs** (#285 c74ac34, #286 a2cf297, #287 ccfdfb4, #288 b7cc84b, #289 956baec); gate reopened.
- Paper trail closed on the merges: Build Record closing comments filed on BIO-0015/0016/0019/0020/0021, all five moved to Done. BIO-0019's trail carries the still-open CEO items (unique constraint dedupe strategy, Railway `DB_QUEUE_RETRY_AFTER` check).
- Carried from shift 006: dry spell broken (consecutive_dry reset).

## Hunt
- `shift-patrol` (rotation 4): ~397k tokens. Post-merge diff-hotspots verified **all five merges clean** (429 retry closure semantics, retry_after arch test, intra-page dedupe, theme shape, password_confirmation round-trip). Gallery-forms swept all 8 useForm consumers — zero defects. **Confirmed: 1** (F-tx-1, verified by the Steward directly after the workflow verifier was lost twice to transient API 529s — verification reasoning recorded on the issue), refuted: 0, low observations: 3.

## Issues Filed
- **BIO-0025** — AssignPartToStorage race guard dead for null color_id: unique index treats NULLs as distinct, concurrent assigns double-insert (medium, Foundry, `bug`).

## Builds
- **BIO-0025 → PR #291** — green: pushed + labeled. `fix/BIO-0025-null-color-assign-race`, commit 64db068, 3 files +271/−0. Partial unique index `WHERE color_id IS NULL` with fail-loudly duplicate pre-check (never deletes/merges); action untouched (existing catch becomes live); 6 DB-real feature tests + endpoint update-path proof. Full gauntlet green (709 tests, 100%/100%, mutation 80.23%). Ops note: Brickwright interrupted mid-mutation by API 529, resumed via SendMessage — completed cleanly, not a build failure.
- **BIO-0023 → PR #290** — green: pushed + labeled. `docs/BIO-0023-sweep-doc-fix-batch`, commit 34162e7, 2 files +6/−6. decisions.md ADR-0028 row → Retired 2026-07-16; Pulse JSDOM + form-data contradictions removed; ADR-count hardcode → pointer; Overall Health refreshed to the sweep's 8.5/7.5 with Assessed dates bumped. All four claims re-verified before editing.

Both issues To Do → In Progress → In Review with PR links commented (branch links 9, 10).

## Observations (low severity, not filed)
1. **P-scan-add-hydrate-1** (new, commented on Done BIO-0016): the restored duplicate warning never fires on cold-store navigation — Add/Scan pages don't `retrieveAll()` on mount (SetDetailPage does); integration spec pre-hydrates, masking it. Recurrence 1; fold into any sets-domain build.
2. **F-tx-2** (new): SyncThemesAction pass-2 parent linking (~600 row updates) has no enclosing transaction — the only write Action without one; mid-loop failure leaves the theme tree partially linked until next sync. Self-healing by design per docblock, but the window is real.
3. **SC-storagemap-202-1** (new): SetDetailPage storage-map fetch doesn't discriminate the 202 sync-pending envelope its endpoint can return (the sibling parts fetch does); currently masked by loadParts' polling order.
4. Carried: F-txn-2/cascade-substring (recurrence 2 — sweep's F-test-1/F-test-2 are adjacent evidence), P-forms-2 (rec. 1), P-recv-3 (rec. 1), P-storageoption-grid (rec. 2).
5. Infra: transient Anthropic API 529s cost one workflow verifier (twice) and one Brickwright mid-gauntlet this shift; both recovered (Steward-direct verification; SendMessage resume). No pattern beyond upstream load.

## For the CEO
**Review queue: 3 — #290 (docs, 2-minute read), #291 (storage race fix with the fail-loudly migration), #284 (the external ui-inputs pilot, oldest).** Note for #291: if its migration pre-check ever aborts on Railway, that means prod already has duplicate null-color rows — the consolidation call is yours. The board is fully current: 5 issues Done with Build Records today, 14 in To Do (BIO-0008 parts-composables is the recommended next build now that #286 merged; BIO-0007/0017/0024 await your decisions).
