# Shift 003 — 2026-07-16

**Steward on duty** · Hunt: patrol rotation 2 (diff-hotspots, gallery-inventory, foundry-receiving, stud-connections) · Loop: shift 3 of run started 2026-07-16

## Roll-Call
- Board at clock-in: 10 in To Do, 0 in progress, 2 in review (BIO-0015/0016 → PRs #285/#286) · Review queue: 3 open agent PRs (#284, #285, #286)
- Carried from last shift: clean handoff. #283 merged by CEO (Pulse refresh landed — `form-data` advisory and ADR-0028 concern both closed; no high-severity actives remain).
- **New external PR noted:** #284 (`WR-0431: adopt @script-development/ui-inputs across BIO (ADR-0043 pilot)`, branch `wr-0431-bio-ui-inputs-pilot`) appeared between shifts, not from a shift — carries the review label; in the CEO's queue. Potential merge-order overlap with #285/#286 (both touch form-input specs); CEO's call.
- Patrol args workaround: ran the patrol via a scratchpad copy with `TODAY`/`ROTATION` pinned (named-workflow args did not reach the script in shifts 001–002). Rotation 2 confirmed executing this time.

## Hunt
- `shift-patrol` (rotation 2): 9 agents (4 finders + 5 verifiers), ~689k tokens, ~12.5 min. **Confirmed: 5, refuted: 0**, low observations: 3.
- Dedupe: S-setnum-1 (AddSetPage dead duplicate warning) = BIO-0016, already fixed in PR #286 — two independent finders converged; skipped.
- diff-hotspots re-verified #279/#280 and the job-leak fixes clean; noted (not filed) a doc nuance: `parentId` goes out already-camelCase from `ResourceData::toArray()` (get_object_vars), so the "backend serializes snake" comment in #279's message is technically wrong but behavior-identical.

## Issues Filed
- **BIO-0017** — PartsPage searches/filters/groups/exports over only loaded cursor pages — wrong totals and hidden parts past 100 location rows (medium, Gallery, `bug`). Left unbuilt: needs a UX scope decision (server-side vs eager-load vs gated), stated on the issue.
- **BIO-0018** — Storage hierarchy is display-only: no parent selector in Add/Edit forms, drawer-in-bin nesting impossible from the UI (medium, Gallery, `inconsistency` — label created this shift, id 45). Feature-shaped; buildable next shift or awaiting CEO product steer on the alternative (strip the dead tree UI instead).
- **BIO-0019** — Queue `retry_after` (90s) below job `#[Timeout(600)]` → concurrent duplicate imports + stranded sets (medium, Foundry, `bug`). Subsumes shift-002 obs. F-tx-1.
- **BIO-0020** — Supplier 429 throttling ignored (Retry-After unread → hard 502/Failed import) (medium, Foundry, `bug`).

## Builds
- **BIO-0019 → PR #287** — green: pushed + labeled. `fix/BIO-0019-queue-retry-after-alignment`, commit 1f728e5, 5 files +142/−9. retry_after 90→700 with invariant documented; intra-page map append fixed; regression test + `QueueRetryAfterArchitectureTest` (enforcement-first verified: fails at 90). Full gauntlet green incl. mutation 80.40%. Deliberately excluded (CEO decisions, commented on issue): unique constraint on `family_sets(family_id, set_id)` (needs prod dedupe strategy), Railway `DB_QUEUE_RETRY_AFTER` env verification, stranded-row recovery.
- **BIO-0020 → PR #288** — green: pushed + labeled. `fix/BIO-0020-supplier-429-backoff`, commit b7d42a3, 4 files +386/−2. 429-aware retry delay in both services (Retry-After honored, 60s clamp, 1s bounded fallback); exception map untouched. 14 new `Sleep::fake()` duration-asserting tests. Full gauntlet green incl. mutation 80.34%.

Both issues To Do → In Progress → In Review with PR links commented. Branch links via `start-work-on-issue` (ids 6, 7).

## Observations (low severity, not filed)
1. **P-storage-tree-depth-3** (Gallery, new): StorageOverviewPage tree renders exactly two levels — `getChildren` never recurses; depth-≥3 locations invisible except via search. Folded into BIO-0018's acceptance criteria rather than filed separately.
2. **P-recv-3** (Foundry, new): `ImportOwnedSetsJob` writes `failed_sets = skipped` but appends the page-level failure only to `failed_set_details` — counts disagree; status can be Failed with failed_sets=0.
3. **S-storagegrid-1** (Foundry/Gallery, new): backend `grid_rows`/`grid_columns` validation + serialization is unreachable from the Gallery (type + store omit the fields), making the parent-grid row/column bounds a permanent no-op — users can submit any row/column ≥ 0. Related to BIO-0018's incomplete-hierarchy theme.
4. Carried at recurrence 1 (shift 001): F-txn-1 set-parts prune gap; F-txn-2 cascade arch test substring match; SC-expires-1 `InviteCode.expiresAt` nullable-vs-typed.
5. Carried at recurrence 2 (patrol plumbing): named-workflow `args` do not reach `shift-patrol.js` (defaults executed in shifts 001–002); worked around via pinned scratchpad copy this shift. One more recurrence of needing the workaround → file an issue on the workflow/args plumbing.
6. Resolved-by-build: P-forms-2 (showcase workbench fake field) still open at recurrence 1; shift-002's other items unchanged.

## For the CEO
**Review queue is now 5 of 6: #284 (external ui-inputs pilot), #285, #286, #287, #288 — one more build slot before the backpressure gate closes the floor to hunt-only.** Suggested order: #287 (data-integrity: stops concurrent-import duplicate/strand mechanism) → #288 (import resilience under supplier throttling) → #286 → #285 → #284. Two decisions parked on your desk: BIO-0017 scope (server-side vs eager-load vs gated search) and BIO-0019's deferred unique constraint + Railway `DB_QUEUE_RETRY_AFTER` check. BIO-0007 (coverage-gate honesty) still awaits your (a)/(b) with fresh evidence attached.
