# Shift 002 — 2026-07-16

**Steward on duty** · Hunt: patrol (ledger rotation 1; see patrol-meta observation — workflow executed rotation 0 dimensions: diff-hotspots, gallery-forms, foundry-transactions, stud-connections) · Loop: shift 2 of run started 2026-07-16, first looped shift (shift 001 was `once` mode)

## Roll-Call
- Board at clock-in: 10 in To Do (BIO-0013 CEO-assigned; BIO-0007 `blocked` on CEO decision; BIO-0001–0006, 0008, 0010 buildable backlog), 0 in progress, 0 in review · Review queue: 1 open agent PR (#283, docs/minutes + Pulse refresh)
- Carried from last shift: clean handoff — PRs #279/#280 merged by CEO. Shift 001's infra blocker is resolved: **GitHub repo now linked to Kendo project 3**; `start-work-on-issue` branch-linking worked all shift (branch links 4 and 5).
- Shift 001's 5 low-severity observations carried; two escalated this shift (see Hunt).

## Hunt
- `shift-patrol` workflow: 6 agents (4 finders + 2 verifiers), ~500k tokens, ~13 min. **Confirmed: 2** (both adversarially verified, both medium, both Gallery-facing), refuted: 0, low-severity observations: 3.
- Both confirmations escalate shift-001 observations: P-form-1 → P-forms-1 (register confirmation error) and SC-setnum-1 → P-dup-1 (phantom `setNum` proven to kill duplicate detection).
- diff-hotspots dimension verified the last 25 commits' code changes clean (#279 theme fix, #280 integration assertions, #281 gate retirement, jobs `failed()` leak fix — no regressions or half-applied renames).

## Issues Filed
- **BIO-0015** — RegisterPage password-confirmation error never renders under the confirmation field (medium, Gallery + Foundry validation rule, `bug`). Deduped: board 0 hits; was an unfiled shift-001 observation.
- **BIO-0016** — Duplicate-set detection permanently dead in AddSetPage/ScanSetPage — compares top-level `setNum` the backend never sends (medium, Gallery, `bug`). Deduped: board 0 hits; type-shape root cause first seen as SC-setnum-1.

## Builds
- **BIO-0015 → PR #285** — green: pushed + label verified. `fix/BIO-0015-password-confirmation-error-key`, commit b8f4d9e, 5 files +98/−8. Backend `RegisterRequest` moves the mismatch to `password_confirmation` (`required|same:password`, `confirmed` dropped); feature test asserts the new key; new frontend integration test proves the message renders under the confirmation input. Both wing gauntlets green (Foundry 687 tests / Gallery 100% + 145 integration). Bonus: fixed a pre-existing Login/Register integration teardown race (spied `goToRoute` promise now awaited).
- **BIO-0016 → PR #286** — green: pushed + label verified. `fix/BIO-0016-duplicate-set-detection`, commit 32055bf, 9 files +161/−34. `(s.set?.setNum ?? s.setNum)` comparison on both pages; `FamilySet.setNum` now optional with new `FamilySetDraft` type (root cause closed at the type layer); unit mocks replaced with real-wire-shape factory; integration spec hydrates the real store through the ADR-0029 middleware chain. Full Gallery gauntlet green (1437 unit / 145 integration).

Both issues To Do → In Progress → In Review with PR links commented. New evidence forwarded to BIO-0007 (coverage-gate honesty): BIO-0016 is a concrete incident of the domain-page coverage exclusion masking a dead guardrail.

## Observations (low severity, not filed)
1. **F-tx-1** (Foundry, new): `ImportOwnedSetsAction` intra-page duplicate `set_num` creates two FamilySet rows (in-memory map never updated after `createFamilySet()`), then the `existingCount > 1` branch permanently skips the set; no unique constraint on `family_sets(family_id, set_id)` backstops it. `ImportOwnedSetsAction.php:119-155,178-210`. Low likelihood (API emits one row per set).
2. **P-forms-2** (Gallery, new): showcase `FormValidationWorkbench` demos `set_number`/`setNumber` — a field the real API doesn't have (`set_num`/`setNum`). Dev-only, but the canonical form-pattern demo teaches a wrong key.
3. **Patrol-meta (recurrence 2 of patrol-plumbing issues):** the workflow ran rotation-0 dimensions and stamped `date: 0000-00-00` despite args `{date: 2026-07-16, rotation: 1}` — the script's arg intake appears broken (shift 001 already noted `galleryDim`/`foundryDim` lockstep indexing). Same dimensions as shift 001 were re-patrolled; still yielded 2 confirmed bugs, but rotation diversity is not happening. One more recurrence → issue on `.claude/workflows` patrol script.
4. Carried at recurrence 1 (shift 001): F-txn-1 set-parts re-sync never prunes removed rows; F-txn-2 cascade arch test is a substring match (illusory guardrail); SC-expires-1 `InviteCode.expiresAt` typed non-null but nullable on the wire; stale `form-data` advisory row in pulse.md (PR #283 in review may already address the Pulse rows).
5. Resolved-by-build: P-dup-2 (FamilySet draft/resource type conflation) closed by BIO-0016's type split. Shift-001 P-form-1 and SC-setnum-1 closed via BIO-0015/BIO-0016.

## Infrastructure Notes
- Branch-linking via `start-work-on-issue` restored (repo id 1 linked to project 3) — degraded path from shift 001 no longer needed.
- Both Brickwright dispatches carried the WSL node-PATH workaround and fresh-worktree provisioning (`npm ci`, backend `cp .env.example .env && composer install`); both gauntlets ran clean on first full pass.

## For the CEO
**Three PRs await review — suggested order: #286, #285, #283.** #286 restores a dead customer-facing guardrail (duplicate-set warning, form + scanner paths); #285 fixes the register-form mismatch error landing under the wrong field (cross-wing: moves the 422 key to `password_confirmation`) and de-flakes the auth integration specs; #283 is Shift-001 docs. The two fix branches touch disjoint files except both ride on `main`'s current state — merge in any order. Board follow-up when convenient: BIO-0007 now has a concrete incident attached and is ripe for your (a)/(b) decision.
