# Shift 012 — 2026-07-17

**Steward on duty** · Hunt: patrol rotation 6 (gallery-inventory + foundry-receiving) · Loop: shift 1 of run re-opened 2026-07-17 (CEO manual re-open after the prior run closed on `consecutive_dry: 2`)

## Pre-shift — halt & recover
Clock-in git gate tripped: the working tree carried an uncommitted `.claude/settings.json` change I didn't author (tightening `Write(.claude/records/**)` + `Write(.claude/docs/**)` → `Edit`-only). Halted per the guardrail (never build on uncommitted work the shift didn't create), reported to the CEO. On the CEO's instruction, committed it onto the open minutes PR **#297** (`chore: tighten records/docs allowlist from Write to Edit-only`, `ba59f61`) and returned `main` to a clean tree, then clocked in.
> Carry-forward: once #297 merges, `Write(.claude/records/shifts/**)` is gone and autonomous Shift-Report filing will prompt. Flagged to CEO — narrow re-add of `Write(.claude/records/shifts/**)` recommended if unattended shifts should keep filing reports.

## Roll-Call
- Board at clock-in: **To Do 13 · In Progress 0 · In Review 6 · Done 9**. Review queue: **1** open agent PR (#297).
- Carried from last run: prior run closed on `consecutive_dry: 2` (two empty shifts). This shift is a fresh CEO re-open.
- Reconciliation done this shift: all **6 "In Review" issues had already merged** (your PR batch #290–#294) but were never closed. Filed Build-Record comments and moved **BIO-0008, 0022, 0023, 0025, 0026, 0027 → Done**. In Review emptied.

## Hunt
- Patrol (8 agents, rotation 6): dimensions `diff-hotspots`, `gallery-inventory`, `foundry-receiving`, `stud-connections`. **4 confirmed, 0 refuted.**
- diff-hotspots clean (recent merges #284/#287/#288/#292/#293/#294 all verified consistent, no half-applied renames, no unmirrored cross-wing changes).
- Triage: `GI-parts-1` = dup of open **BIO-0017**; `GI-storage-1/2` already inside open **BIO-0018**; one genuinely new medium filed (below). Remainder low → observations.

## Issues Filed
- **BIO-0029** — Stranded Pending/InProgress ImportJob permanently locks a family out of importing — no reclamation path (medium, Foundry, bug). New; distinct from Done BIO-0019/0020/0021.

## Builds
- **BIO-0029 → PR #298** [green: pushed + `Agent Review Requested`]. `StartImportAction` staleness-reclamation (flip stale active job `Failed` inside the insert transaction; config-driven `IMPORT_STALE_ACTIVE_JOB_THRESHOLD`, default 1200s). Full Foundry gauntlet + 100% coverage + pre-push green. Cancel-endpoint/reaper deferred to CEO.
- **BIO-0001 → RED, no push, returned to To Do + `blocked`.** The Work Order premise is false: the 7 showcase specs do **not** `vi.mock`+`findComponent` `SectionHeading` — they pass the *real* component through `shallowMount`'s `stubs` and assert on its rendered text, so the top-level import is **load-bearing**. Brickwright proved it (trial removal → `TS18004` + `ReferenceError`, then reverted; tree clean). No one-line mechanical path exists; the real fix is a 7-file `vi.mock` refactor the WO explicitly forbade. Diagnosis + verified ground-truth paths commented on the issue; parked pending a rescope decision.

## Unfiled Findings
*(none blocked by a permission gate — all board writes succeeded this shift)*

## Observations (low severity, not filed — batched; 3 recurrences → issue)
- **P-recv-2** (Foundry, low): `GetSetPartsAction` re-dispatches only on `Failed`; a set stranded `Pending`/`InProgress` (worker down at dispatch, or lost job row) never re-kicks the sync — client polls forever. Same stranded-async-state family as BIO-0029; natural follow-up to bundle.
- **P-recv-3** (Foundry, low): `GetSetPartsAction` Failed/new-set paths have no concurrency guard — duplicate `SyncSetPartsJob` dispatch possible.
- **P-recv-4** (Foundry, low): `RebrickableService` caches an empty parts list at the full 24h TTL — poisons re-sync for a day.
- **SC-import-1** (cross-wing, low, inconsistency): `SettingsPage.importSets()` maps HTTP 422 → "configure your token" and 403 → "not family head", but `StartImportAction` only ever throws 409 (`ImportAlreadyInProgress`); the missing-token check lives in the async job (throws 400 inside `ImportOwnedSetsJob`). A token-less import returns 202, the poll sees `failed` and silently stops with no message. The 422 branch is dead; needs a which-wing-moves decision (surface job-failure reason vs synchronous pre-flight token check).
- **GI-place-1** (Gallery, low): `PlacePartModal` submits unclamped quantity (typed 0 / negative / over-max bypass input constraints).

## Series-wide flag
The arch-cleanup series **BIO-0002…0006** was migrated from May-2026 Work Orders and may share BIO-0001's false "dead import" premise for any non-`*Mocked` spec. Each batch's claim should be re-verified (real-component-`stubs` vs `vi.mock`) before pickup. Noted on BIO-0001.

## For the CEO
1. **Review PR #298** (BIO-0029 import-lockout fix) — the shift's one buildable win, green and labeled.
2. **BIO-0001 needs a rescope call** — it's `blocked`; the whole arch-cleanup series is premise-suspect. Decide: rescope to a real `vi.mock` refactor, or drop the series.
3. Housekeeping: #297 (your minutes PR) now also carries the settings-tightening commit; merging it removes autonomous Shift-Report write access unless you re-add `Write(.claude/records/shifts/**)`.

Review queue after this shift: **PR #297, PR #298**.
