# Shift 004 — 2026-07-16

**Steward on duty** · Hunt: patrol rotation 3 (diff-hotspots, gallery-contract-types, foundry-exceptions-queue, stud-connections) · Loop: shift 4 of run started 2026-07-16

## Roll-Call
- Board at clock-in: 12 in To Do, 0 in progress, 4 in review (BIO-0015/0016/0019/0020 → PRs #285-#288) · Review queue: 5 open agent PRs (#284-#288)
- Carried from last shift: clean handoff. No merges since shift 003.
- **Backpressure discipline:** at 5 of 6, the shift self-capped to 1 build (2 would overshoot the CEO's review throttle to 7).
- Patrol again run via pinned scratchpad copy (rotation 3 confirmed in launch log) — args-plumbing workaround now standing practice, recorded in the ledger.

## Hunt
- `shift-patrol` (rotation 3): 7 agents, ~523k tokens, ~11 min. **Confirmed: 3, refuted: 0**, low observations: 2.
- **Convergence signal:** 2 of 3 confirmations re-found in-review work — P-familyset-setnum = BIO-0016 root cause (PR #286), F-queue-1 = BIO-0019 (PR #287). Evidence comments added to both issues (the `set?` optional-but-always-sent delta on BIO-0016; single-worker-topology nuance + optional `WithoutOverlapping` hardening on BIO-0019). The hunt is beginning to saturate on rotations 0–3's territory; shift 005's full sweep arrives on schedule.
- diff-hotspots verified #279/#281/#262/#259/#260 clean. Noted: stale worktree `agent-a62e1a1ceffde46d8` under `.claude/worktrees/` still carries pre-retirement PrePushPermitGate references (not live tree; cleanup candidate).

## Issues Filed
- **BIO-0021** — Queue Worker docs claim `--timeout=60` while both real jobs run under `#[Timeout(600)]` (medium, Foundry docs, `inconsistency`+`documentation`).
- **BIO-0022** — InviteCode.expiresAt/createdAt typed non-null but Foundry emits null for non-expiring codes; SettingsPage unguarded (low, Gallery, `inconsistency`). Filed on third independent sighting per the recurrence rule (SC-expires-1: shifts 001, 003-carry, 004×2).

## Builds
- **BIO-0021 → PR #289** — green: pushed + labeled. `docs/BIO-0021-queue-timeout-doc-reconcile`, commit 99495d3, 2 files +3/−1 (docs only). Both Queue Worker sections now document the `#[Timeout]`-overrides-`--timeout` precedence and the retry_after invariant, no stale-able values cited. Hooks ran full Foundry gauntlets (pre-commit CaptainHook + pre-push composer test 687 tests) — green.

One build by design (backpressure self-cap). Issue To Do → In Progress → In Review, PR link commented, branch link id 8.

## Observations (low severity, not filed)
1. **P-storageoption-grid / SC-storage-grid-1** (recurrence 2, was S-storagegrid-1 shift 003): backend emits `grid_rows`/`grid_columns`, frontend type/adapter omit them — plus new detail this shift: EditStoragePage's patch would null out grid fields on any API-seeded option. One more sighting → issue (or fold into BIO-0018 if the CEO green-lights hierarchy work).
2. Stale worktree `.claude/worktrees/agent-a62e1a1ceffde46d8` (pre-ADR-0028-retirement vintage) — harmless but pollutes greps; candidate for `git worktree prune`/removal in a hand-run session (shift write-surface excludes it).
3. Carried at recurrence 1: F-txn-1 (set-parts prune gap), F-txn-2 (cascade arch test substring match), P-forms-2 (showcase fake field), P-recv-3 (failed_sets undercount), P-storage-tree-depth-3 (folded into BIO-0018 acceptance).
4. Resolved-by-filing: SC-expires-1 → BIO-0022.

## For the CEO
**Review queue is now 6 of 6 — the gate is closed; shifts build nothing until you merge.** Queue: #284 (external ui-inputs pilot), #285, #286, #287, #288, #289. Suggested order unchanged: #287 → #288 → #286 → #285 → #289 → #284 (note #289 is a 2-minute docs read that pairs with #287). **Shift 005 is the scheduled full warden sweep (~1.4M tokens, the cadence dial in the ledger is yours)** — it will hunt and file only, which the closed gate enforces anyway. Decisions still parked: BIO-0017 scope, BIO-0019's deferred unique constraint + Railway `DB_QUEUE_RETRY_AFTER` check, BIO-0007 (a)/(b).
