# Shift 001 — 2026-07-16

**Steward on duty** · Hunt: patrol rotation 0 (diff-hotspots, gallery-forms, foundry-transactions, stud-connections) · Loop: shift 1 of run started 2026-07-16 (`once` mode — single shift, no loop)

## Roll-Call
- Board: 13 in To Do at clock-in (1 assigned to CEO: BIO-0013; 1 `blocked` awaiting CEO decision: BIO-0007; BIO-0011 self-deferred pending ADR-0028 amendment BIO-0012), 0 in progress, 0 awaiting CEO · Review queue: 0 open agent PRs
- Carried from last shift: maiden shift — no predecessor. Pre-shift gate failure earlier today (dirty tree: uncommitted minutes addendum) resolved via PR #278, merged by CEO before clock-in.

## Hunt
- shift-patrol workflow (rotation 0): 5 finders, 438k tokens, ~15 min. **Confirmed: 1** (adversarially verified), refuted in verify: 0, low-severity observations: 5 (batched below), patrol artifacts discarded: 1 (P-hot-1 — the finder read this shift's own live clock-in edit of LEDGER.md in the working tree and attributed it to commit f32e8e2; the committed seed I read at clock-in was correctly `idle`/`shift: 0`. Refuted by direct observation.)

## Issues Filed
- **BIO-0014** — Set theme wire-shape drift: backend sends object, frontend types string — grouped Sets view crashes (high, Gallery/cross-wing, `bug`). Deduped against board (0 hits) before filing.

## Builds
- **BIO-0014 → PR #279** — green: pushed + labeled. `fix/BIO-0014-theme-wire-shape-drift`, commit 815f71b, 13 files +42/−22. Frontend-only: new `Theme` interface, retyped `SetSummary.theme`/`SetWithParts.theme`, 5 consumer read-sites → `theme?.name`, 13 wrong-shape fixtures converted to real wire shape. Full gauntlet green incl. 100% coverage (117 files/1435 tests) + integration (143 tests).
- **BIO-0009 → PR #280** — green: pushed + labeled. `test/BIO-0009-integration-flow-assertions`, commit 7be14a1, 14 files +125/−22. mock-server.ts records `{method, endpoint, body}` post-middleware (snake_case, ADR-0029) with `calls()`/`callsTo()`; all 13 flow tests assert request-fired and/or navigation side effects; 4 form-POST flows assert exact snake_case bodies with camelCase `not.toHaveProperty` guards. Full gauntlet green. Kept orthogonal to BIO-0014 (theme fixtures untouched) to avoid merge conflicts.

Both issues moved To Do → In Progress → In Review with PR links commented. No PrePushPermitGate involvement (both under threshold).

## Observations (low severity, not filed)
1. **P-form-1** (Gallery): RegisterPage confirm-password binds `errors.passwordConfirmation`, a key the backend never emits (Laravel `confirmed` rule attaches to `password`) — mismatch errors render under the wrong field. `RegisterPage.vue:66` / `RegisterRequest.php:31`.
2. **F-txn-1** (Foundry): set-parts re-sync upserts but never prunes rows removed upstream — orphaned `set_parts` silently inflate completion denominators. `StoreSetPartsAction.php:45-141`. Low probability (near-immutable upstream inventories) but unrecoverable without manual DB cleanup.
3. **F-txn-2** (Foundry): `cascadeRelations()` has no runtime engine; the arch test "delete actions handle all cascade relations" is a `str_contains` substring match — a `->load()` mention passes it. Illusory guardrail. `CascadeRelationArchitectureTest.php:113-154`.
4. **SC-setnum-1** (Gallery): `FamilySet.setNum` is a phantom top-level field the backend never sends; consumers all defend via `set?.setNum` so no live failure, but the type lies.
5. **SC-expires-1** (Gallery): `InviteCode.expiresAt` typed non-null but backend sends `null` when invite TTL ≤ 0; SettingsPage renders it unguarded.
6. (Pulse hygiene, not patrol) `form-data` advisory (GHSA-hmw2-7cc7-3qxx) listed Open in pulse.md is already resolved — lockfile has 4.0.6, outside the 4.0.0–4.0.5 range. Pulse row is stale.
7. (Patrol meta) shift-patrol.js indexes `galleryDim` and `foundryDim` both by `rotation % 4` — dimension pairs are locked in lockstep. No starvation, but combination diversity is reduced.

## Infrastructure Notes
- **Kendo project 3 has no primary GitHub repository linked** — `start-work-on-issue`/`link-branch` fail with "Project has no primary GitHub repository linked." Degraded to `update-issue` (assign+lane) + branch name in a comment, per charter. CEO action: link the repo in Kendo to restore branch-linking.
- Non-interactive shells resolve `npx`/`npm` to the Windows install (`/mnt/c/Program Files/nodejs/`), breaking the commitlint hook and any node tooling; fix is prepending `~/.nvm/versions/node/v24.15.0/bin` to PATH. Both Brickwright dispatches carried the workaround; both also needed `npm ci` (fresh worktrees have no `node_modules`).

## For the CEO
**PRs #279 and #280 await your review** — #279 first: it fixes a high-severity, customer-visible crash (grouped Sets view, the app's default landing render for themed sets). The two branches are file-disjoint by design; merge in either order. Board follow-ups when convenient: link the GitHub repo to Kendo project 3, and BIO-0012 (ADR-0028 amendment) still needs your interrogation session.
