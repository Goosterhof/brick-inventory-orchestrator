# Minutes — 2026-05-28 — Opus 4.8 Harness Review

_Board meeting note between the CEO and The Steward._
_Captured by the Meeting Minutes Secretary (1x1 translucent-clear brick, with clipboard)._

---

## 2026-05-28 — Opus 4.8 Harness Review

### Decisions

- **Review framing**: A harness is compensation for model limitations; an Opus 4.8 bump shifts *which* limitations bind. The review sorted harness pieces into keep (cheap safety rails), reconsider (discipline-nudges), fix (one concrete bug), and CEO-decision (governance).
- **Thread 1 — backend hook worktree bug**: Fixed `--git-directory=../.git` (invalid in a worktree where `.git` is a file) via `git rev-parse --git-common-dir`. Scope extended beyond the WO (pre-commit only) to `pre-push`, which carried the identical defect.
- **Thread 2 — minutes fragmentation**: New minutes are per-session files in `.claude/records/minutes/`; `MINUTES.md` frozen as the archive (through 2026-05-28) rather than moved, to preserve historical cross-links. SessionEnd auto-commit repointed at the directory.
- **Thread 3 — bypass-log clause**: CEO chose to **drop** the ADR-0028 `--no-verify` bypass-log clause entirely (option c), not the lighter relax-to-`/minutes` option (b). Recorded as settled doctrine in Amendment 2026-05-28 (II); gate mechanics unchanged.
- **Thread 3 — trims**: Removed the two advisory Stop nudge hooks (self-described no-ops); bumped Quality Warden `sonnet` → `opus`.

### Friction Signals

- One commit-boundary correction: a bare `git commit` swept already-staged `git rm` deletions into the ADR commit, misattributing them relative to the chore commit's message. Fixed in one round via `git reset --soft` + recommit (no push had happened yet).

### Dynamics

- CEO selected all three threads to pursue (AskUserQuestion), then made four follow-up calls in a second question round. On the bypass clause the CEO took the harder line (drop, not rescope), citing the retro's zero-honored-in-two-weeks evidence.

### Process Meta

- `Explore` subagent dispatched once to map the harness (settings, hooks, agents, skills, CI, dispatchers).
- `/minutes` fired (this file) — first use of the new per-session format.
- **SessionEnd auto-commit hook fired mid-session**, committing the `MINUTES.md` banner edit as standalone commit `695369e` before it was manually staged. Surfaced as a behavior note: the auto-commit triggers at harness-decided boundaries, not only true session end.
- No `--no-verify` used; `core.hooksPath` is unset in the fresh container, so no gauntlet fired locally.
- **Verification limitation**: container has Node 22 / PHP 8.4 / no `backend/vendor`, so CaptainHook could not be run. The backend hook fix was verified at the git-dir-resolution level in a real worktree only.
- Branch `claude/opus-4.8-review-7V3Cu` pushed; 5 commits. CEO opened the PR.
- **Reconciliation (2026-05-29, open-PR review):** a parallel Opus-4.8 session shipped **PR #138** that independently fixed the *same* `pre-commit` worktree bug under the *same* WO — and ran CaptainHook end-to-end (Rector 9→341 files), closing the verification gap above. Deconfliction: merged #138 first, rebased this PR onto `main`, dropped the duplicate `pre-commit` hunk, kept only the unique `pre-push` sibling fix + governance, and aligned `pre-push` to #138's idiom. Headline learning: parallel harness-review sessions need WO claiming to avoid forging the same brick twice.

### Notes

- ADR-0028's mechanical gate (refuse over-threshold backend pushes lacking an open WO) is untouched by the bypass-log retirement — only the after-the-fact logging tax was removed.
- Other agents (`brickwright`, `steward`, `pattern-master`) already float on the `opus` alias, so they pick up 4.8 automatically without edits.

### Action Items

- [x] ~~Run CaptainHook in a real `git worktree` on a PHP-8.5 machine~~ — **done by PR #138** (Rector 9→341 files); transfers to `pre-push` via the byte-identical resolution.
- [x] ~~Close WO `2026-05-28-backend-pre-commit-worktree-safety` post-merge~~ — **closed by PR #138** (the WO it satisfies). This PR's BR re-scoped to the `pre-push` sibling.
- [ ] **Steward**: Babysit CI on PR #140 after the rebase push; merge when green.

### Open Questions

- A code-bearing `--no-verify` bypass now leaves no mandatory record. Accepted as a deliberate trade (gate fails closed on the high-risk case; CI/review catch the rest). Revisit only if a concrete incident shows a mandatory log would have prevented it.

---
