# Component Spec: [ComponentName]

## Purpose

_What brick is this? Where does it click into the build?_

## Props

| Prop      | Type     | Required | Default | Description               |
| --------- | -------- | -------- | ------- | ------------------------- |
| `example` | `string` | Yes      | —       | _What this prop controls_ |

## Emits

| Event     | Payload  | Description             |
| --------- | -------- | ----------------------- |
| `example` | `string` | _When this event fires_ |

## Slots

| Slot      | Props | Description              |
| --------- | ----- | ------------------------ |
| `default` | —     | _What goes in this slot_ |

## States

- **Default**: _How the brick looks on the baseplate_
- **Loading**: _Waiting for data — loading indicator, disabled interactions_
- **Error**: _Red border (`border-red-500`), red shadow, error message below_
- **Disabled**: _Reduced opacity, no interactions, cursor-not-allowed_

## Brick Brutalism Compliance

- [ ] `border="3 black"` on interactive surfaces
- [ ] Hard offset shadow (`4px` default, `6px` hover/focus, `2px` active)
- [ ] Focus: shadow grows + `bg-yellow-300`
- [ ] No `border-radius` (unless this is a stud — studs are round)
- [ ] Error state uses red shadow + red border, not just color change

## Accessibility

- **Role**: _ARIA role if non-standard element_
- **Keyboard**: _Tab, Enter, Escape behaviors_
- **Screen reader**: _aria-label, aria-describedby, live regions_

## Usage Example

```vue
<ComponentName example="value" @example="handleExample" />
```
